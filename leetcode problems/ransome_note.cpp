#include<bits/stdc++.h>
using namespace std;


bool isPossible(string rn, string mg) {
     unordered_map<char , > map_;
     for(auto i: mg) {
          map_()
     }
}

int main() {
     string rn, mg;
     cin >> rn >> mg;

     cout << (int)isPossible(rn, mg);
}